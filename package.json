{
  "name": "decentralized-verification-system",
  "version": "1.0.0",
  "description": "Decentralized Certificate and Land Verification System using Blockchain",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "deploy": "cd contracts && npx hardhat run scripts/deploy.js --network goerli",
    "test": "cd contracts && npx hardhat test"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "solidity",
    "certificate",
    "land",
    "verification",
    "ipfs",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
